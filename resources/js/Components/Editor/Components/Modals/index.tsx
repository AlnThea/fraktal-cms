import React from 'react';

// This component is currently empty as modals are handled in Fullscreen mode
// Regular modals are managed by GrapesJS itself
const Modals: React.FC = () => {
    return null;
};

export default Modals;
